[global]

    workgroup = SYSTEMVIEWINC
    client signing = yes
    client use spnego = yes
    kerberos method = secrets and keytab
    realm = CORP.SYSTEMVIEWINC.COM
    security = ads

    log file = /var/log/samba/log.%m
    max log size = 1000
    syslog = 0

    server role = member server
